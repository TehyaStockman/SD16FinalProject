import numpy as np
from scipy.spatial.distance import pdist, squareform

fishes = np.zeros(1,30)

def positions(n):
	# return np.array([px,py], dtype=np.float64) for px in range(0,n) and py in range(0,n)
print positions(10)

# velocity = np.array([vx,vy])
# acceleration = np.array([ax,av])

# motion = np.array([position,velocity,acceleration])